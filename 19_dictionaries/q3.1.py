# Create a tuple coordinates = (10, 20) and print both elements.

coordinates = (10, 20)
print(coordinates[0])
print(coordinates[1])

coordinates[0] = 50  # Try to modify the tuple by setting coordinates[0] = 50 — it will give error