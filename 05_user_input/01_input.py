# The input() function allows the user to take input from the keyboard.
# By default, the input() function takes the input as a string & also converts into another data types as needed.

a = input("Enter a no.:")
print(a)

b = input("Enter your name: ")
print(b)
